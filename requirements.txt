# DELIBERATELY VULNERABLE - Plumber campaign fixture (lane B2).
#
# Every pin below (a) installs cleanly on the interpreter the generator selects,
# and (b) has a HIGH or CRITICAL advisory with a fix available, so Trivy reports it
# even with --ignore-unfixed and the deterministic dependency-bump remediation has
# somewhere to go. An earlier revision pinned versions that could not install at all
# (Django 2.2 on py3.12, urllib3 1.24 conflicting with requests), which failed the
# BUILD gate instead of the SCAN gate and so tested the wrong thing.
Django==4.2.30
gunicorn==22.0.0
dj-database-url==2.1.0
psycopg2-binary==2.9.9
whitenoise==6.6.0
requests==2.31.0
Pillow==12.3.0
aiohttp==3.13.3
cryptography==48.0.1
setuptools==78.1.1